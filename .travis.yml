sudo: false
language: java
jdk:
  - openjdk8

cache:
  directories:
    - $HOME/.m2
    
deploy:
   provider: script
   script: .travis/release.sh
   skip_cleanup: true
   on:
    repo: ConsumerDataStandardsAustralia/cds-codegen
    tags: true
    jdk: openjdk8


